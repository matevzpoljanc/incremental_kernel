(library
 ((name incremental_kernel)
  (inline_tests ((javascript build_and_run)))
  (public_name incremental_kernel)
  (libraries (core_kernel))
  (preprocess (pps (ppx_jane -check-doc-comments)))
  (preprocessor_deps (debug.mlh))
  (js_of_ocaml ())))

(enforce_style ((exceptions (import.ml))))

(jbuild_version jane_street)
